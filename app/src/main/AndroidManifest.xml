<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coordinacion">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!-- MainActivity ahora está en el paquete activities -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MenuPrincipalActivity -->
        <activity
            android:name=".activities.MenuPrincipalActivity"
            android:exported="false"
            android:label="@string/menu_principal_activity_title"/>

        <!-- Nuevas Actividades -->
        <activity
            android:name=".activities.SeleccionarCategoriaActivity"
            android:exported="false"
            android:label="@string/seleccionar_categoria_activity_title"/>

        <activity
            android:name=".activities.ListaPersonasActivity"
            android:exported="false"
            android:label="@string/lista_personas_activity_title"/>

        <activity
            android:name=".activities.EliminarRegistrosActivity"
            android:exported="false"
            android:label="@string/eliminar_registros_activity_title"/>

        <!-- AgregarPersonaActivity -->
        <activity
            android:name=".activities.AgregarPersonaActivity"
            android:exported="false"
            android:label="@string/agregar_persona_activity_title"/>

    </application>

</manifest>