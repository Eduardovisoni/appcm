<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_app"
    tools:context=".activities.ListaPersonasActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewPersonas"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingBottom="16dp"
        android:paddingTop="60dp"
        android:clipToPadding="false"/>

    <!-- Mensaje de estado vacío (oculto por defecto) -->
    <TextView
        android:id="@+id/tvEmptyState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:text="No hay ninguna persona registrada.\nRegresa y presiona en el botón 'Agregar Persona'"
        android:textColor="@color/text_secondary"
        android:textSize="18sp"
        android:padding="32dp"
        android:visibility="gone"/>

</RelativeLayout>